import styled from 'styled-components';
// import '../index.css';

// Wrapper

const JoinCodeTitle = styled.p`
    font-size: 18px;
    color: var(--secondary-color);
    background-color: var(--primary-color);
    text-decoration: underline;
    font-weight: 700;
`;

const Code = styled.p`
  font-size: 1.5em;
  text-align: center;
  color: palevioletred;
`;

export { Code, JoinCodeTitle };
