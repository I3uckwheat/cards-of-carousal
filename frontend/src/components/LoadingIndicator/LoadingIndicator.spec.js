import React from 'react';
import { render, screen } from '@testing-library/react';
import LoadingIndicator from './LoadingIndicator';

describe('LoadingIndicator', () => {
  describe('rendering', () => {
    // snapshot
    // renders in correct color when true
    // renders in correct color when false
    // renders in correct color when no prop
  });

  describe('propTypes', () => {
    // logs an error if prop is not a boolean
    // does not log an error if no prop is passed
  });
});
