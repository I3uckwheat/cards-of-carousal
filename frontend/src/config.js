const configValues = {
  maxPlayersValues: {
    min: 2,
    max: 12,
    default: 8,
  },

  winningScoreValues: {
    min: 1,
    max: 15,
    default: 7,
  },

  initialSelectedPack: [10],

  cardHandSize: 10,
};

export default configValues;
